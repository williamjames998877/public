 language: ruby


addons:
  postgresql: 9.6

rvm: 2.6.3

cache: bundler


jobs:
  include:
    - os: windows
      language: c
      arch: amd64
      vm:
        size: large
    - stage: "rspec"
      script: bundle exec rspec
      before_script:
        - "bundle exec rake db:create db:migrate"